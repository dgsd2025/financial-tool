# 财务中心 · 运行说明

> **部署 = 注册到星逸平台门户。** 具体步骤见 [`接入星逸平台.md`](./接入星逸平台.md)。
> 本文只讲进程怎么起停。

---

## 当前状态

| 项 | 值 |
|---|---|
| 访问地址 | http://localhost:5190 |
| 应用目录 | `~/Documents/系统开发/yc-finance-web` |
| 进程方式 | 普通后台进程（与招得快、ZuJiPro 等系统一致） |
| 开机自启 | **无**（已按要求移除 launchd 托管，避免与 dev-stack 重复） |
| 依赖 | 无。不需要 `npm install`，无构建步骤 |

端口 5190 的原因：5173（门户）/ 5180（招得快）/ 5181（ZuJiPro）/ 3000（培训）已被占用。

---

## 起停

```bash
cd ~/Documents/系统开发/yc-finance-web
./fin.sh start | stop | restart | status | logs
```

`fin.sh` 是幂等的：

- 重复 `start` 不会起两个进程
- 端口被别的程序占用时**直接报错，不硬抢**
- 可以安全地放进 `dev-stack.sh` 的批量启动流程

改端口：`FIN_PORT=5191 ./fin.sh restart`

---

## 接入 dev-stack.sh

我读不到 `dev-stack.sh`（macOS 限制了 `Documents` 读权限），这段要你自己贴：

```bash
# --- 财务中心 ---
FIN_DIR="$BASE/yc-finance-web"
fin_start()  { "$FIN_DIR/fin.sh" start; }
fin_stop()   { "$FIN_DIR/fin.sh" stop; }
fin_status() { "$FIN_DIR/fin.sh" status; }
```

---

## 数据安全

- 银行流水**只在浏览器里解析，不上传任何服务器**
- `server.js` 只做静态文件分发，不接收、不存储任何业务数据
- 规则库与处理记录存浏览器 `localStorage`

> 接入门户 SSO 时第一件事：把规则库挪到服务端。它是这个工具最值钱的沉淀——
> 用半年能攒出几百条拿真账练出来的规则，直接就是 M2 自动记账的底稿。
> 存 localStorage 换台电脑就没了，多人也没法共用。

---

## 目录

```
yc-finance-web/
├── index.html          外壳
├── app.css             样式（亮/暗双主题）
├── app.js              全部逻辑：系统结构 + 工具箱 + T2
├── lib/xlsx-lite.js    零依赖 xlsx / csv 解析
├── server.js           零依赖静态服务器
├── fin.sh              起停脚本
├── package.json
├── 示例文件/银行流水_示例.csv
├── README.md           功能与设计
├── 接入星逸平台.md      ← 部署主文档
└── 运行说明.md          本文
```
