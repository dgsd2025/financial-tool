# 接入星逸平台（xingyi.vvaix.com）

> 门户源码我读不到（macOS 限制了 `Documents` 读权限），但门户是 Vite 起的开发服务器——
> **源码通过 HTTP 暴露**，所以我从 `http://localhost:5173/src/...` 把结构完整读出来了。
> 下面的字段值全部来自门户真实代码，不是猜的。

---

## 门户的系统注册机制

摸清楚了：

| 项 | 结论 |
|---|---|
| 技术栈 | Vue 3 + Pinia + Element Plus + UnoCSS + Vite |
| 工作台 | `/apps` → `src/views/apps/index.vue` |
| 系统卡来源 | 后端 API `GET /api/v1/system/my` → `{ opened, locked }` |
| 管理入口 | 管理后台 → **子系统管理**（`/admin/system`） |
| 新增接口 | `POST /api/v1/system/save` |
| 后端状态 | 已在跑（`/api/v1/tenant/by-domain` 返回 200，租户 `aole` = 星逸平台） |

**系统卡是后端数据库里的记录，不是前端写死的**——所以注册一次就永久生效，不用改任何代码。

---

## 关键字段的含义（来自门户源码注释）

```ts
/** 0=仅跳转（不签票） 1=门户跳转 auth_code 2=OAuth2 3=SAML */
ssoProtocol: 0 | 1 | 2 | 3
/** 1=仅电脑 2=仅手机 3=两者 */
formFactor: 1 | 2 | 3
```

工作台把系统分成两组：

- `opened`（可点）：已开通 **或 `ssoProtocol=0`** 或当前用户是管理员
- `locked`（灰掉）：`showInPortal=1` 且 `status=1` 且 `ssoProtocol≠0` 且未开通

> **财务中心目前用 `ssoProtocol=0`**（仅跳转、不签票）。
> 原因：这个应用现在**没有鉴权**，用户信息是写死的。用 0 的好处是所有人不用「开通」就能点进去，
> 等以后接了 auth_code 再改成 1。截图里「企业门户信息」那张卡显示「跳转 · 需自行登录」，
> 就是 `ssoProtocol=0` 的效果。

---

## 注册方式一：管理后台点一下（推荐）

**管理后台 → 子系统管理 → 新增**，按下表填：

| 字段 | 值 |
|---|---|
| 系统名称 | `财务中心` |
| 系统编码 | `finance` |
| 应用ID (AppID) | `finance` |
| 访问地址 (url) | `http://localhost:5190` |
| 手机地址 (urlMobile) | 留空 |
| 图标 (icon) | `财` |
| 描述 | `工具箱 · 银行流水转凭证` |
| SSO 协议 | **0 — 仅跳转（不签票）** |
| 终端形态 | **1 — 仅电脑** |
| 启用 SSO | **否**（ssoEnabled = 0） |
| 门户显示 | **是**（showInPortal = true） |
| 排序 | `90` |

其余字段（callbackUrl / rsaPublicKey / clientId / clientSecret / 各类账号同步 URL）**全部留空**——
那些是 `ssoProtocol ≥ 1` 才用得上的。

**终端形态选「仅电脑」的理由**：这个工具的核心动作是把银行流水 Excel 拖进来，
界面是宽表格（凭证 15 列），手机上没法用。等以后有适合手机的功能再改成「两者」。

---

## 注册方式二：控制台一行搞定

如果懒得填表单：**在已登录的门户页面**（`http://localhost:5173/apps`）按 F12 打开控制台，粘贴运行：

```js
await fetch('/api/v1/system/save', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer ' + localStorage.getItem('access_token')
  },
  body: JSON.stringify({
    name: '财务中心', code: 'finance', appId: 'finance',
    url: 'http://localhost:5190', urlMobile: '',
    icon: '财', description: '工具箱 · 银行流水转凭证',
    ssoProtocol: 0, formFactor: 1, ssoEnabled: 0,
    showInPortal: true, sort: 90,
    callbackUrl: '', rsaPublicKey: '', clientId: '', clientSecret: '',
    visibleScope: 1, visibleConfig: '',
    createAccountUrl: '', freezeAccountUrl: '', resetPasswordUrl: '',
    syncUrl: '', logoutUrl: '', guideType: 0, guideUrl: ''
  })
}).then(r => r.json()).then(console.log)
```

跑完刷新 `/apps`，第 9 张卡就出来了。

> 这段用的是**你浏览器里已有的登录态**（`access_token`），我全程没有接触任何账号密码或令牌。
> 我也无法代你执行——需要登录态的操作只能你自己来。

---

## 网络拓扑：本地能通，域名还不行

实测：

| 域名 | 解析 |
|---|---|
| `xingyi.vvaix.com` | ✅ 可解析 |
| `finance.vvaix.com` | ❌ 不可解析（还没建） |

`/etc/hosts` 里也没有 vvaix 相关记录。

所以：

- **本机访问门户 → 点财务中心 → `http://localhost:5190`**，通的
- **别的设备通过 `xingyi.vvaix.com` 访问门户 → 点财务中心 → localhost 指向那台设备自己**，打不开

### 要走公网/内网域名的话，还需要

1. 建 `finance.vvaix.com` 的 DNS 记录
2. 配反向代理指到这台机器的 5190
3. 把 url 从 `http://localhost:5190` 改成 `https://finance.vvaix.com`

这三步涉及你们的 DNS 和服务器，我做不了。**先按 localhost 用起来，等要多设备访问再配。**

---

## 已做的适配

「返回门户」按钮已接好（`app.js` 的 `portalUrl()`）：

- 门户跳转时若带 `?from=` 参数，就回跳到该地址
- 没带就回退到 `http://localhost:5173/apps`

如果门户跳转链接改成 `http://localhost:5190/?from=http://localhost:5173/apps`，返回体验会更连贯。

---

## 后续接 SSO（免登直达）

现在是「跳转 · 需自行登录」，但财务中心根本没有登录页，所以点进去直接就是工作台。
等要做真正的免登直达（像其他 7 个系统那样），要做三件事：

1. 子系统管理里把 `ssoProtocol` 改成 **1**，`ssoEnabled` 改成 **1**，填 `callbackUrl`
2. 财务中心实现 auth_code 消费：接收门户带过来的 code，调后端换用户信息
3. 把 `app.js` 里写死的用户（潘海鹏 · 财务经理）改成从 SSO 结果取

同时「成员与开通」里给相关岗位开通该系统，`locked` 组的人才能点。

---

## 检查清单

- [ ] 财务中心服务在跑：`./fin.sh status`
- [ ] 子系统管理里新增「财务中心」（方式一或方式二）
- [ ] 刷新 `/apps`，确认第 9 张卡出现
- [ ] 点卡片能打开 `http://localhost:5190`
- [ ] 点财务中心左上角 logo 能回到门户
